package com.example.demo.entity.common;

public enum StatusCode {
    WELL_DONE,
    ALREADY,
    NOT_FOUND,
    INVALID_PARAMETER,
    INTERNAL_ERROR,
    UNKNOWN,
    SUCCESS,
    ACCEPTED,
    FAILURE,
    EXPIRED
}
